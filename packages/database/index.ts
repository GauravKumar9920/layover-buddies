// The generated artifact lives inside the Edge Function tree because Supabase
// deploys that tree in isolation. This workspace is the browser-safe re-export.
export type { Database, Json } from '../../supabase/functions/_shared/database.types';
