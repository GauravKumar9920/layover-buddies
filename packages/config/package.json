{
  "name": "@detour/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Detour configuration — design tokens (theme) and business-rule constants.",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "type-check": "tsc --noEmit"
  }
}
